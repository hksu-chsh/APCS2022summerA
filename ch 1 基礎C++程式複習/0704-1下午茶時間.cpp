#include <bits/stdc++.h>
using namespace std;
int main(){
	int st = 14 * 60 + 20, ed = 16 * 60 + 40;
	int h, m, cur;
	cin >> h >> m;
	cur = h * 60 + m;
	cout << (a <= cur && cur <= b ? "YES" : "NO");
}